{
  "name": "snake-war",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Serpent's Ascent Stage 1: browser-playable 3D baby snake survival prototype for GitHub Pages.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@babylonjs/core": "9.15.0"
  },
  "devDependencies": {
    "vite": "8.1.3"
  },
  "engines": {
    "node": ">=22"
  }
}
